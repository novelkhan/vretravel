import axios from 'axios';

const showNotification = (isSuccess, title, message) => {
  // Implement notification logic here
};

const openExpiringSessionCountdown = () => {
  // Implement expiring session countdown logic here
};

export default {
  showNotification,
  openExpiringSessionCountdown,
};